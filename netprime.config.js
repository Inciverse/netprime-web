// NetPrime — user stream configuration
// Edit the URLs below to YOUR AUTHORIZED streaming source.
// The player builds:  STREAM_URL + "/movie/" + id   (or "/tv/")
// Leave a value empty ("") if you don't have one.
// This file is optional: if absent, the app falls back to /api/config.
window.__NETPRIME_USER_CONFIG__ = {
  streamUrl: "https://vidbinge.dev/embed",        
  streamUrlFallback: "https://vidsrc.cc/v2/embed" 
};
